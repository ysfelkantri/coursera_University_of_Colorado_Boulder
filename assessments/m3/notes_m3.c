/////////////////////////////memory////////////////////////////////


"data memory (RW)
	stack : store temporary data like local variables 

	heap : dynamic data allocation 

	data : non zero initialized global and static data 

	bss : block started by symbol - uninitialized and zero initialized global and static data 

"code memory (RX) :
	intvecs 
	text : 	
	const/ rodata : for const qualifier Read only 	 
	cinit :
	pinit : 

"register 


storage classes :
auto : par defaulyt kiktebtiha ki la 
static : persist to the lifetime of program : local scope of access
extern : can accessed by the file1 from yhe file2 which is declared into
register : attribute the variable directly to a register 
